const addBtn = document.getElementById('btn');

const parentText = document.getElementById('text');

addBtn.addEventListener('click', () => {
    const childText = document.createElement('li');
    setTimeout(() => {
        text.textContent = 'ボタンをクリックしました';
    }, 2000);
    
});